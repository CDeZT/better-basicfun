# Third-party notices

The calm effort-slider interaction and visual structure in `lib/client.js` are
adapted from `dsh-reasoning-effort` / `dsh-better-reasoning-effort` by
HanaAyane and contributors, released under the MIT License.

The vendored client bundle remains part of this plugin so DSH can load one
package. The bundle has been renamed to `better-basicfun`, its native-provider
path is gated, the manual rows are exposed behind **高级设置**, and the visual
effects are reduced while retaining the slider animation.
